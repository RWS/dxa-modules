matrix:
  include:
    - language: java
      sudo: false
      jdk:
        - openjdk8
      script:
      install: 
        - skip
      os: 
        - linux
      dist: 
        - trusty
      script:
        - git update-index --chmod=+x gradlew
        - chmod +x gradlew
        - git ls-tree HEAD
        - cd webapp-java
        - ./gradlew build  
    - language: csharp
      solution: webapp-net/DxaModules.sln            
