/**
 * Delay used for setTimeout to put the check at the end of render queue
 */
export const RENDER_DELAY = 0;

/**
 * Delay used to emulate async call to service
 */
export const ASYNC_DELAY = 10;

/**
 * Delay used to check async call to service
 */
export const ASYNC_TEST_DELAY = ASYNC_DELAY + 1;
