@model UgcComments
<div>
    <span>Showing @Model.Comments.Count comments</span>
    <div class="list-group">
        @foreach(var comment in Model.Comments)
        {
            <p>@comment.User.Name</p>
            <time class="meta small">@Html.DateDiff(comment.LastModifiedDate.DateTime)</time>
            <p>@comment.Content</p>
        }
    </div>
</div>